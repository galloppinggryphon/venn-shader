type FormattingConfig = {
   canvas: {
      background: string;
      activeBackground: string;
   };
   selected: {
      background: string;
   };
   graphs: {
      backgroundColor: string | string[];
      borderColor: string;
      borderWidth: number;
   };
   sets: {
      font: {
         font: string;
         size: number;
      };
   };
   intersections: {
      font: {
         font: string;
         size: number;
      };
   };
};
